# Юнит-тесты. Подготовка и проблемы

- [Юнит-тесты. Подготовка и проблемы](#юнит-тесты-подготовка-и-проблемы)
  - [Именование файлов с тестами](#именование-файлов-с-тестами)
  - [Отключите конфликт с Pytest в настройках](#отключите-конфликт-с-pytest-в-настройках)
  - [Где находится конфигурация тестов в VS Code](#где-находится-конфигурация-тестов-в-vs-code)
  - [Требования для запуска тестов](#требования-для-запуска-тестов)
  - [Как запустить тест](#как-запустить-тест)
    - [🚀 Способы запуска в Visual Studio Code](#-способы-запуска-в-visual-studio-code)


## Именование файлов с тестами

Главная причина, почему тест не отображается в списке — **дефисы (`-`) в имени файла `test_integration-3-10.py**`.

В панели **OUTPUT** на первом скриншоте видна ошибка:
`Failed to import test module... ModuleNotFoundError`

Для поиска и запуска тестов фреймворк `unittest` импортирует файлы как Python-модули. В Python дефис `-` является оператором вычитания, поэтому имена файлов с дефисами **не являются валидными именами модулей**, и `unittest` падает с ошибкой импорта, не успевая построить дерево тестов.

* **Было:** `test_integration-3-10.py`
* **Стало:** `test_integration_3_10.py`

---

## Отключите конфликт с Pytest в настройках

На вашем втором скриншоте (в `settings.json`) одновременно включены и `unittest`, и `pytest`:

```json
"python.testing.unittestEnabled": true,
"python.testing.pytestEnabled": true

```

Когда включены оба фреймворка, тестовый адаптер VS Code начинает путаться.

1. Откройте Настройки (`Ctrl + ,`).
2. Введите в поиск `python testing pytest enabled`.
3. Снимите галочку с **Python > Testing: Pytest Enabled** (оставьте только **Unittest Enabled**).

---

## Где находится конфигурация тестов в VS Code

Во вкладке **Testing** (колба слева) настройки не живут — там отображается только сформированное дерево тестов.

Конфигурация тестов в VS Code запускается и меняется двумя способами:

1. **Через Палитру команд:**

* Нажмите `Ctrl + Shift + P` (или `Cmd + Shift + P` на Mac).
* Наберите **Python: Configure Tests**.
* Выберите **unittest** $\rightarrow$ корневую папку `.` $\rightarrow$ шаблон `test_*.py`.

1. **В файле `settings.json` (который у вас уже настроен):**
У вас в файле `.vscode/settings.json` уже прописаны правильные аргументы для запуска:

```json
"python.testing.unittestEnabled": true,
"python.testing.unittestArgs": [
    "-v",
    "-s",
    ".",
    "-p",
    "test_*.py"
]

```

## Требования для запуска тестов

Запущенный Docker.

`pip install docker psycopg2-binary`

test_integration_3_10.py

## Как запустить тест

Да, это классический **интеграционный тест**, использующий подход паттерна *Testcontainers* (через официальный Python SDK для Docker). Он автоматически поднимает изолированный контейнер PostgreSQL 17, настраивает схему базы данных, проводит CRUD-тесты и удаляет контейнер за собой.

---

### 🚀 Способы запуска в Visual Studio Code

1. **Запуск через интерфейс Testing (Рекомендуемый):** Использование встроенного Test Explorer в VS Code.
1. Установите официальное расширение **Python** от Microsoft, если оно ещё не установлено.
1. Откройте боковую панель **Testing** (иконка в виде лабораторной колбы на панели слева).
1. Если тесты не вывелись автоматически, нажмите **Configure Python Tests** $\rightarrow$ выберите **unittest** $\rightarrow$ укажите корневую папку $\rightarrow$ выберите шаблон имени (например, `*test*.py`).
1. В появившемся дереве нажмите иконку **Play (Run Test)** напротив файла или конкретного метода (например, `test_insert_table`).

1. **Прямой запуск файла кнопкой Run / F5:**
В вашем коде в конце присутствует блок:

```python
if __name__ == "__main__":
    unittest.main()

```

Это значит, что файл можно запустить как обычный Python-скрипт:

* Нажмите иконку **Play** в правом верхнем углу редактора.
* Или нажмите `F5` / `Ctrl + F5` для запуска в режиме отладки или без неё.

1. **Запуск через встроенный терминал VS Code:**
Откройте терминал (`Ctrl + ~` или `Cmd + ~`) и выполните команду:

```bash
python path/to/your_file.py

```

Или через штатный модуль unittest:

```bash
python -m unittest path/to/your_file.py

```
