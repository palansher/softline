# Конкатенация - это присоединение через оператор +

info = "Привет!" #создается первый объект
info += " Как " #динамически выделяется память под два объекта
info += "дела?" #динамически выделяется память под три объекта

print(info)